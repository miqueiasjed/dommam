# Task 1.5 – Migrations: downloads, webhooks_log, admin_users, admin_audit_log

## Objetivo
Criar as migrations das quatro tabelas de suporte: registro de downloads, log de webhooks Lastlink, usuários admin e log de auditoria das ações administrativas.

## Tipo
config

## Arquivos a criar/modificar
- `backend/database/migrations/xxxx_create_downloads_table.php` → criar
- `backend/database/migrations/xxxx_create_webhooks_log_table.php` → criar
- `backend/database/migrations/xxxx_create_admin_users_table.php` → criar
- `backend/database/migrations/xxxx_create_admin_audit_log_table.php` → criar

## Contexto necessário (arquivos para ler antes)
- `backend/database/migrations/` → ver migrations existentes (criadas nas tasks 1.1 e 1.4)

## Skills necessárias
- `laravel-arquitetura`

## Especificação

### O que fazer:

#### Migration 1: downloads
```php
// php artisan make:migration create_downloads_table --create=downloads

Schema::create('downloads', function (Blueprint $table) {
    $table->id();
    $table->foreignId('roteiro_id')->constrained('roteiros')->cascadeOnDelete();
    $table->string('email');
    $table->string('ip_address', 45)->nullable();
    $table->text('user_agent')->nullable();
    $table->timestamps();

    $table->index('email');
    $table->index('roteiro_id');
});
```

#### Migration 2: webhooks_log
```php
// php artisan make:migration create_webhooks_log_table --create=webhooks_log

Schema::create('webhooks_log', function (Blueprint $table) {
    $table->id();
    $table->string('evento');
    $table->json('payload');
    $table->enum('status', ['pendente', 'processado', 'erro'])->default('pendente');
    $table->text('erro_mensagem')->nullable();
    $table->timestamp('processado_em')->nullable();
    $table->timestamps();

    $table->index('status');
    $table->index('evento');
});
```

#### Migration 3: admin_users
```php
// php artisan make:migration create_admin_users_table --create=admin_users

Schema::create('admin_users', function (Blueprint $table) {
    $table->id();
    $table->string('nome');
    $table->string('email')->unique();
    $table->string('password');
    $table->string('totp_secret')->nullable();
    $table->boolean('ativo')->default(true);
    $table->timestamp('ultimo_acesso')->nullable();
    $table->rememberToken();
    $table->timestamps();

    $table->index('email');
    $table->index('ativo');
});
```

#### Migration 4: admin_audit_log
```php
// php artisan make:migration create_admin_audit_log_table --create=admin_audit_log

Schema::create('admin_audit_log', function (Blueprint $table) {
    $table->id();
    $table->foreignId('admin_id')->constrained('admin_users')->cascadeOnDelete();
    $table->string('acao');
    $table->string('entidade_tipo')->nullable();
    $table->unsignedBigInteger('entidade_id')->nullable();
    $table->json('dados_antes')->nullable();
    $table->json('dados_depois')->nullable();
    $table->string('ip_address', 45)->nullable();
    $table->timestamps();

    $table->index('admin_id');
    $table->index(['entidade_tipo', 'entidade_id']);
    $table->index('created_at');
});
```

### Regras aplicáveis:
- Colunas de negócio em **português** (`processado_em`, `ultimo_acesso`, `ativo`, etc.)
- `admin_users` é separado de membros — sem relação com a tabela `sessions`
- A tabela `downloads` usa `email` (string), não FK para evitar dependência com Lastlink

### Critérios de aceitação:
- [ ] Todas as 4 migrations criadas
- [ ] FKs corretas: `downloads.roteiro_id` → `roteiros`, `admin_audit_log.admin_id` → `admin_users`
- [ ] `php artisan migrate` executa todas sem erros
- [ ] Índices criados conforme especificação

## Teste esperado
- `cd backend && php artisan migrate --pretend` → mostrar SQL das 4 tables sem erros
- `cd backend && php artisan migrate` → executar com sucesso
- `cd backend && php artisan migrate:status` → todas as 8 migrations como "Ran"

## Estimativa de complexidade
média
~100 linhas de código | ~4 arquivos
