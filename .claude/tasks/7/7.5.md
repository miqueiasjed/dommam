# Task 7.5 – BibliotecaPage: Integração Final

## Objetivo
Substituir o placeholder de `BibliotecaPage` pela implementação real: compor `SearchBar`, `SortSelector`, `FilterBar` e `RoteiroGrid` usando o hook `useRoteiros`, com layout responsivo e sticky da barra de controles.

## Tipo
pagina

## Arquivos a criar/modificar
- `frontend/src/pages/BibliotecaPage.jsx` → modificar (substituir placeholder)

## Contexto necessário (arquivos para ler antes)
- `frontend/src/hooks/useRoteiros.js` → ver o que o hook expõe
- `frontend/src/components/biblioteca/SearchBar.jsx` → props esperadas
- `frontend/src/components/biblioteca/SortSelector.jsx` → props esperadas
- `frontend/src/components/biblioteca/FilterBar.jsx` → props esperadas
- `frontend/src/components/roteiro/RoteiroGrid.jsx` → props esperadas
- `frontend/src/components/layout/AppShell.jsx` → ver estrutura do shell

## Skills necessárias
- frontend-design-system

## Especificação

### O que fazer:

Substituir o conteúdo de `frontend/src/pages/BibliotecaPage.jsx` pela implementação completa:

```jsx
import AppShell from '../components/layout/AppShell';
import { useRoteiros } from '../hooks/useRoteiros';
import SearchBar from '../components/biblioteca/SearchBar';
import SortSelector from '../components/biblioteca/SortSelector';
import FilterBar from '../components/biblioteca/FilterBar';
import RoteiroGrid from '../components/roteiro/RoteiroGrid';

export default function BibliotecaPage() {
  const { roteiros, carregando, erro, filtros, setFiltros, busca, setBusca, ordenacao, setOrdenacao } = useRoteiros();

  return (
    <AppShell>
      <div className="flex flex-col h-full">
        {/* Barra de controles sticky */}
        <div className="sticky top-0 z-10 bg-page border-b border-zinc-800/80 px-6 py-3 space-y-2">
          <div className="flex items-center gap-3 flex-wrap">
            <SearchBar busca={busca} onBuscaChange={setBusca} />
            <SortSelector ordenacao={ordenacao} onOrdenacaoChange={setOrdenacao} />
          </div>
          <FilterBar filtros={filtros} onFiltrosChange={setFiltros} roteiros={roteiros} />
        </div>

        {/* Grid de roteiros */}
        <div className="flex-1 overflow-auto px-6 py-6">
          <RoteiroGrid roteiros={roteiros} carregando={carregando} erro={erro} />
        </div>
      </div>
    </AppShell>
  );
}
```

**Notas:**
- A `SearchBar` tem `min-w-[200px]` ou `flex-1` para ocupar espaço disponível
- `bg-page` é o token definido em `index.css` — usar sempre em vez de hardcode
- A barra sticky usa `top-0` porque o `main` do AppShell já tem `overflow-auto`

### Regras visuais:
- Usar `bg-page` para fundo da sticky bar (não `bg-[#030406]`)
- Manter espaçamento `px-6` consistente com o resto do AppShell

### Critérios de aceitação:
- [ ] BibliotecaPage monta sem erros no console
- [ ] Barra de busca e filtros aparecem na parte superior
- [ ] Grid de roteiros renderiza abaixo da barra
- [ ] Scroll funciona na área do grid (sticky bar permanece visível)
- [ ] Filtros e busca propagam para o grid (via hook)
- [ ] Build sem erros: `npm run build`
- [ ] Dev server funciona: `npm run dev`

## Teste esperado
- Build sem erros
- Abrir `/biblioteca` no navegador e verificar que a página carrega com barra de filtros e grid (com skeletons se API não disponível)

## Estimativa de complexidade
baixa
~35 linhas | ~1 arquivo
