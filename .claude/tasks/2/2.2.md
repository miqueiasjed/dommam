# Task 2.2 – LastlinkService + EmailService

## Objetivo
Criar o `LastlinkService` (wrapper para API Lastlink com cache Redis) e o `EmailService` (serviço de envio de e-mail para magic link).

## Tipo
integracao

## Arquivos a criar/modificar
- `backend/app/Services/Auth/LastlinkService.php` → criar
- `backend/app/Services/Email/EmailService.php` → criar

## Contexto necessário (arquivos para ler antes)
- `backend/config/services.php` → entender onde configurar credenciais de serviços externos
- `backend/config/mail.php` → entender configuração de e-mail atual
- `.claude/skills/laravel-arquitetura/SKILL.md` → padrões do projeto

## Skills necessárias
- `laravel-arquitetura` (leia `.claude/skills/laravel-arquitetura/SKILL.md`)

## Especificação

### LastlinkService (`backend/app/Services/Auth/LastlinkService.php`):

**Responsabilidade:** verificar se um e-mail possui assinatura ativa na Lastlink, com cache Redis.

```php
namespace App\Services\Auth;

use Illuminate\Support\Facades\Cache;
use Illuminate\Support\Facades\Http;
use Illuminate\Support\Facades\Log;

class LastlinkService
{
    private string $baseUrl;
    private string $apiKey;
    private int $cacheTtlSegundos;

    public function __construct()
    {
        $this->baseUrl = config('services.lastlink.url', 'https://api.lastlink.com');
        $this->apiKey = config('services.lastlink.api_key', '');
        $this->cacheTtlSegundos = (int) config('services.lastlink.cache_ttl', 600); // 10 min padrão
    }

    public function assinaturaAtiva(string $email): bool
    {
        $chave = 'lastlink:assinatura:' . md5(strtolower($email));

        return Cache::remember($chave, $this->cacheTtlSegundos, function () use ($email) {
            return $this->consultarLastlink($email);
        });
    }

    private function consultarLastlink(string $email): bool
    {
        try {
            $response = Http::withToken($this->apiKey)
                ->timeout(5)
                ->get("{$this->baseUrl}/api/v1/subscribers/{$email}");

            if ($response->successful()) {
                $status = $response->json('status');
                return in_array($status, ['active', 'trialing']);
            }

            if ($response->status() === 404) {
                return false; // e-mail não encontrado na Lastlink = sem assinatura
            }

            Log::warning('Lastlink retornou status inesperado', [
                'email' => $email,
                'status_http' => $response->status(),
            ]);

            return false;
        } catch (\Exception $e) {
            Log::error('Falha ao consultar Lastlink', ['email' => $email, 'erro' => $e->getMessage()]);
            return false; // fail closed — sem acesso em caso de falha
        }
    }

    public function invalidarCache(string $email): void
    {
        $chave = 'lastlink:assinatura:' . md5(strtolower($email));
        Cache::forget($chave);
    }
}
```

Também adicionar em `config/services.php` a entrada:
```php
'lastlink' => [
    'url'       => env('LASTLINK_API_URL', 'https://api.lastlink.com'),
    'api_key'   => env('LASTLINK_API_KEY', ''),
    'cache_ttl' => env('LASTLINK_CACHE_TTL', 600),
],
```

E adicionar as variáveis no `.env.example`:
```
LASTLINK_API_URL=https://api.lastlink.com
LASTLINK_API_KEY=
LASTLINK_CACHE_TTL=600
```

### EmailService (`backend/app/Services/Email/EmailService.php`):

**Responsabilidade:** enviar e-mail de magic link (e outros e-mails futuros do projeto).

```php
namespace App\Services\Email;

use Illuminate\Support\Facades\Mail;
use Illuminate\Support\Facades\Log;

class EmailService
{
    public function enviarMagicLink(string $email, string $token): void
    {
        $link = config('app.frontend_url') . '/auth/verificar?token=' . $token;

        Mail::send([], [], function ($message) use ($email, $link) {
            $message
                ->to($email)
                ->subject('Seu link de acesso ao Danuzio History Backstage')
                ->html($this->templateMagicLink($link));
        });

        Log::info('Magic link enviado', ['email' => $email]);
    }

    private function templateMagicLink(string $link): string
    {
        return <<<HTML
        <h2>Acesse o Danuzio History Backstage</h2>
        <p>Clique no link abaixo para entrar. O link expira em <strong>15 minutos</strong>.</p>
        <p><a href="{$link}" style="background:#b45309;color:#fff;padding:12px 24px;text-decoration:none;border-radius:6px;">Acessar agora</a></p>
        <p style="color:#666;font-size:12px;">Se você não solicitou este link, ignore este e-mail.</p>
        HTML;
    }
}
```

Adicionar `FRONTEND_URL` em `config/app.php` e `.env.example`:
```php
'frontend_url' => env('FRONTEND_URL', 'http://localhost:5173'),
```

### Regras visuais aplicáveis:
- `fail closed` para Lastlink — em caso de erro, nega acesso (mais seguro)
- Cache Redis com TTL configurável via `.env`
- E-mail com `log` driver em dev (driver padrão do Laravel)

### Critérios de aceitação:
- [ ] `LastlinkService::assinaturaAtiva()` usa `Cache::remember()` com chave baseada no e-mail
- [ ] `LastlinkService::invalidarCache()` remove o cache do e-mail
- [ ] Em caso de erro na API Lastlink, retorna `false` (fail closed) e registra log
- [ ] `EmailService::enviarMagicLink()` gera link com token e envia via `Mail::send()`
- [ ] Configurações Lastlink centralizadas em `config/services.php` + `.env.example`
- [ ] `config/app.php` inclui `frontend_url`

## Teste esperado
- Build sem erros: `cd backend && composer dump-autoload`
- Em dev, e-mail vai para `log` driver (ver `storage/logs/laravel.log`)
- Em dev, cache vai para `file` driver (não requer Redis rodando)

## Estimativa de complexidade
média
~90 linhas de código | ~2 arquivos (+modificações em config)
