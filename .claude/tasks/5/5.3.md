# Task 5.3 – DownloadService (orquestrador do fluxo de download)

## Objetivo
Criar o `DownloadService` que orquestra todo o fluxo de download: busca o PDF master no Storage, chama `MarcaDaguaService`, registra o download no banco e dispara alerta quando > 50 downloads em 24h do mesmo e-mail.

## Tipo
integracao

## Arquivos a criar/modificar
- `backend/app/Services/Download/DownloadService.php` → criar

## Contexto necessário (arquivos para ler antes)
- `backend/app/Models/Download.php` → ver model criado na Task 5.1
- `backend/app/Models/Roteiro.php` → ver campo `pdf_url` e `slug`
- `backend/app/Services/Download/MarcaDaguaService.php` → ver assinatura do método `aplicar()`
- `backend/app/Services/Auth/SessaoService.php` → entender o que a sessão expõe (email, nome)

## Skills necessárias
- laravel-arquitetura

## Especificação

### O que fazer:

```php
namespace App\Services\Download;

use App\Models\Download;
use App\Models\Roteiro;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\Storage;

class DownloadService
{
    public function __construct(
        private readonly MarcaDaguaService $marcaDagua,
    ) {}

    public function baixar(Roteiro $roteiro, string $email, string $nome, string $ip): string
    {
        // 1. Buscar PDF master do Storage
        $pdfMaster = Storage::get($roteiro->pdf_url);
        abort_if(!$pdfMaster, 404, 'PDF não encontrado');

        // 2. Aplicar marca d'água personalizada
        $pdfPersonalizado = $this->marcaDagua->aplicar($pdfMaster, $email, $nome);

        // 3. Registrar o download no banco
        Download::create([
            'roteiro_id' => $roteiro->id,
            'email'      => $email,
            'ip_address' => $ip,
            'user_agent' => request()->userAgent(),
        ]);

        // 4. Verificar limite de downloads (alerta de abuso)
        $this->verificarLimite($email);

        return $pdfPersonalizado;
    }

    private function verificarLimite(string $email): void
    {
        $total = Download::porEmail($email)
            ->where('created_at', '>=', now()->subHours(24))
            ->count();

        if ($total > 50) {
            Log::warning('Limite de downloads excedido', [
                'email'      => $email,
                'downloads'  => $total,
                'periodo'    => '24h',
            ]);
        }
    }
}
```

### Regras arquiteturais:
- Não usar `Auth::user()` — recebe email e nome via parâmetro (vêm do middleware)
- Não cachear o PDF gerado (cada download é único)
- `Log::warning` apenas, não bloqueia o download — auditoria manual
- Toda operação de storage via `Storage::get()` do Laravel

### Critérios de aceitação:
- [ ] Classe `DownloadService` criada em `App\Services\Download\`
- [ ] Método `baixar()` orquestra as 4 etapas em ordem
- [ ] Download registrado em `downloads` a cada chamada bem-sucedida
- [ ] `Log::warning` disparado quando > 50 downloads em 24h do mesmo e-mail
- [ ] Retorna bytes do PDF com marca d'água (string)
- [ ] Nenhuma referência a `Auth::user()` ou `request()` além do `userAgent()`

## Teste esperado
- Verificar que `Download::count()` aumenta após chamada
- Verificar que `Log::warning` é chamado quando `Download::porEmail` retorna > 50

## Estimativa de complexidade
média
~55 linhas de código | ~1 arquivo
