# Task 5.1 – Model Download

## Objetivo
Criar o Model Eloquent `Download` com seus relacionamentos, aproveitando a migration `create_downloads_table` já existente.

## Tipo
config

## Arquivos a criar/modificar
- `backend/app/Models/Download.php` → criar

## Contexto necessário (arquivos para ler antes)
- `backend/database/migrations/2026_05_13_225133_create_downloads_table.php` → entender colunas disponíveis
- `backend/app/Models/Roteiro.php` → modelo de referência de padrão Eloquent do projeto

## Skills necessárias
- laravel-arquitetura

## Especificação

### O que fazer:
Criar `backend/app/Models/Download.php` com:
- `$fillable`: `['roteiro_id', 'email', 'ip_address', 'user_agent']`
- Relationship `roteiro()` → `belongsTo(Roteiro::class)`
- Casts: nenhum necessário (campos são strings simples)
- **Não** usar SoftDeletes — downloads são log imutável
- Scope `porEmail(string $email)` para facilitar consultas de auditoria

### Critérios de aceitação:
- [ ] Classe `Download` criada em `App\Models\Download`
- [ ] `$fillable` correto com as 4 colunas
- [ ] Relacionamento `roteiro()` retorna `BelongsTo` para `Roteiro`
- [ ] Scope `porEmail` definido
- [ ] Sem regras de negócio no model

## Teste esperado
- `php artisan tinker` → `Download::porEmail('x@y.com')->count()` retorna 0 sem erros
- `php artisan migrate:status` → tabela `downloads` aparece como "Ran"

## Estimativa de complexidade
baixa
~30 linhas de código | ~1 arquivo
