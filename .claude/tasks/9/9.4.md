# Task 9.4 — AdminRoteirosPage + useAdminRoteiros

## Objetivo
Página de listagem de roteiros no painel admin com filtros e ações.

## Arquivos a criar
- `frontend/src/pages/admin/AdminRoteirosPage.jsx`
- `frontend/src/hooks/useAdminRoteiros.js`

## useAdminRoteiros.js
```js
// Estado: { roteiros, carregando, erro, filtroStatus, setFiltroStatus }
// filtroStatus: 'todos' | 'publicado' | 'agendado' | 'rascunho'
// GET /api/admin/roteiros?status={filtroStatus}
// Funções: despublicar(id) → PATCH /api/admin/roteiros/{id} { status: 'rascunho' }
// deletar(id) → DELETE /api/admin/roteiros/{id}
```

## AdminRoteirosPage — Visual

### Header da página
```
Roteiros                          [+ Novo roteiro]
Gerencie os roteiros do Backstage
```
- Título: `font-mono text-[11px] uppercase tracking-widest text-zinc-500` + "Roteiros" em `text-xl text-white font-semibold`
- Botão novo: `bg-amber-500 hover:bg-amber-400 text-black font-mono text-xs uppercase tracking-widest px-4 py-2 rounded-lg`

### Filtros de status
- 4 tabs: Todos | Publicados | Agendados | Rascunhos
- Tab: `font-mono text-xs uppercase tracking-widest`
- Ativo: `text-amber-400 border-b-2 border-amber-400`
- Inativo: `text-zinc-500 hover:text-zinc-300`

### Tabela
- Container: `bg-[#0d0d0f] border border-amber-500/10 rounded-xl overflow-hidden`
- Header linha: `bg-[#111214] text-[10px] font-mono uppercase tracking-widest text-zinc-500`
- Colunas: Título | Status | Publicação Instagram | Disponível no Portal | Ações
- Status badge:
  - publicado: `bg-emerald-500/10 text-emerald-400 border border-emerald-500/20`
  - agendado: `bg-amber-500/10 text-amber-400 border border-amber-500/20`
  - rascunho: `bg-zinc-800 text-zinc-500 border border-zinc-700`
- Ações por linha: ícone Edit (link para editar) + ícone Trash (despublicar com confirmação)
- Responsivo: em mobile ocultar colunas de datas

### Estado vazio/loading
- Loading: spinner amber centralizado na tabela
- Vazio: "Nenhum roteiro encontrado" em zinc-500

## Status: pendente
