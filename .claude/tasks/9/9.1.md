# Task 9.1 — AdminAuthContext + useAdminAuth + AdminProtectedRoute

## Objetivo
Criar contexto de autenticação admin separado do contexto de membros, hook useAdminAuth e componente AdminProtectedRoute.

## Arquivos a criar/editar
- `frontend/src/contexts/AdminAuthContext.jsx` (criar)
- `frontend/src/hooks/useAdminAuth.js` (criar)
- `frontend/src/components/admin/AdminProtectedRoute.jsx` (criar)

## Especificação

### AdminAuthContext.jsx
```jsx
// Estado: { adminEmail, isAdminAuthenticated, carregando }
// Endpoints:
//   GET /api/admin/me → verifica sessão admin
//   POST /api/admin/login → login com email+senha
//   POST /api/admin/2fa/verificar → verifica TOTP
//   POST /api/admin/logout
// Exporta: AdminAuthContext (createContext), AdminAuthProvider
```

### useAdminAuth.js
```js
// import useContext(AdminAuthContext)
// Lança erro se usado fora do Provider
```

### AdminProtectedRoute.jsx
```jsx
// Se carregando → mostra Spinner centralizado (fundo #050606)
// Se não autenticado → Navigate to="/admin/login" replace
// Se autenticado → renderiza children
```

## Status: pendente
