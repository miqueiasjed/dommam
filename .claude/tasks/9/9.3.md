# Task 9.3 — AdminLayout + App.jsx routing

## Objetivo
Sidebar responsiva do painel admin e atualização das rotas.

## Arquivos a criar/editar
- `frontend/src/components/admin/AdminLayout.jsx` (criar)
- `frontend/src/App.jsx` (editar — adicionar AdminAuthProvider e rotas admin)

## AdminLayout — Especificação

### Visual (inspirado no geopolitica AdminLayout)
- Fundo geral: `bg-[#050606]`
- Sidebar: `w-56` desktop, collapsible mobile
- Sidebar bg: `bg-[#0d0d0f] border-r border-amber-500/10`
- Logo area: "DANUZIO HISTORY" em `font-mono text-[10px] uppercase tracking-[0.32em] text-amber-500/60` + "Admin" em `text-amber-400 font-semibold`
- Nav items: `font-mono text-xs uppercase tracking-[0.14em]`
  - Inativo: `text-zinc-500 hover:bg-white/4 hover:text-zinc-200`
  - Ativo: `bg-amber-500/10 text-amber-400`
- Footer sidebar: link "← Voltar ao portal" + botão "Sair"

### Itens de navegação
```js
const itens = [
  { label: 'Roteiros', rota: '/admin/roteiros', icone: FileText },
  { label: 'Assinantes', rota: '/admin/assinantes', icone: Users },
  { label: 'Audit Log', rota: '/admin/audit-log', icone: Activity },
]
```

### Responsivo
- Mobile: hamburger menu → drawer overlay (estado useState `sidebarAberta`)
- Desktop (lg+): sidebar fixa

### App.jsx — mudanças
1. Importar `AdminAuthProvider` de `../contexts/AdminAuthContext`
2. Envolver toda a árvore de rotas com `<AdminAuthProvider>`
3. Substituir rota `/admin/*` atual por:
```jsx
<Route path="/admin/login" element={<AdminLoginPage />} />
<Route path="/admin/2fa" element={<AdminTotpPage />} />
<Route path="/admin" element={<AdminProtectedRoute><AdminLayout /></AdminProtectedRoute>}>
  <Route index element={<Navigate to="/admin/roteiros" replace />} />
  <Route path="roteiros" element={<AdminRoteirosPage />} />
  <Route path="roteiros/novo" element={<RoteiroFormPage />} />
  <Route path="roteiros/:id/editar" element={<RoteiroFormPage />} />
  <Route path="assinantes" element={<AdminAssinantesPage />} />
  <Route path="audit-log" element={<AdminAuditLogPage />} />
</Route>
```

## Status: pendente
