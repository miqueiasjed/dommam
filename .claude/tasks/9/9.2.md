# Task 9.2 — AdminLoginPage + AdminTotpPage

## Objetivo
Páginas de autenticação do admin: email+senha e TOTP de 6 dígitos.

## Arquivos a criar
- `frontend/src/pages/admin/AdminLoginPage.jsx`
- `frontend/src/pages/admin/AdminTotpPage.jsx`

## Paleta Visual (geopolitica + amber Danuzio)
- Background: `#050606`
- Surface card: `#0d0d0f`  
- Border card: `rgba(245, 158, 11, 0.15)` (amber sutil)
- Accent: `amber-500` (#f59e0b)
- Texto: `#f7f7f2`
- Inputs: bg `#111214`, border `rgba(245,158,11,0.2)`, focus ring amber
- Font label: `font-mono text-[10px] uppercase tracking-widest text-zinc-500`

## AdminLoginPage
- Layout: `min-h-screen flex items-center justify-center bg-[#050606]`
- Card: `bg-[#0d0d0f] border border-amber-500/15 rounded-xl p-8 w-full max-w-sm`
- Header: ícone `Shield` (lucide) + "Danuzio History" + badge "Admin"
- Campos: email, senha (type="password")
- Botão: `bg-amber-500 hover:bg-amber-400 text-black font-mono font-semibold uppercase tracking-widest`
- POST /api/admin/login → sucesso redireciona para /admin/2fa

## AdminTotpPage
- Mesma estrutura visual do Login
- Input único de 6 dígitos (type="text" maxLength=6, pattern=\d{6})
- POST /api/admin/2fa/verificar → sucesso redireciona para /admin/roteiros
- Botão "Voltar" → /admin/login

## Status: pendente
